<Page x:Class="lab3.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:lab3"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <ScrollViewer>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                <TextBlock Text="¡Hola, Uno Platform!" FontSize="24" HorizontalAlignment="Center"/>
                <Button Content="Presióname" Click="OnButtonClick"/>
                <TextBlock x:Name="MessageText" Text="" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

    </ScrollViewer>
    <Page.DataContext>
        <local:MainViewModel/>
    </Page.DataContext>
</Page>
